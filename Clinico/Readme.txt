
INSTRUCTIONS FOR RUNNING THE DEPLOYED PROGRAM:

1.Navigate to the directory using cd.
2.Once you are in the directory of the jar file run:
	(a) First run the server by the command : "java -jar Server.jar serverDir"
		(serverDir := path to server directory)
	(b) Then you can run the clients in whatever order you wand by:
		ClientA: "java -jar ClientA.jar"
		ClientB: "java -jar ClientB.jar"
